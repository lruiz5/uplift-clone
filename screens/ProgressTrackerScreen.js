import * as React from "react";
import { View, Text } from "react-native";

export const ProgressTrackerScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Progress Tracker Screen</Text>
    </View>
  );
};
